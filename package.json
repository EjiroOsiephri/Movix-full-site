{
  "name": "root-project",
  "version": "1.0.0",
  "scripts": {
    "install-backend": "cd backend && npm install",
    "start-backend": "cd backend && npm start"
  },
  "dependencies": {
    "express": "^4.18.2",
    "react-toastify": "^10.0.5"
  }
}
